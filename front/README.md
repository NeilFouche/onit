# front

This app serves the website (front). It provides an API for the frontend to get access to the database.

It contains classes to handle navigation data transformations.

The request handlers are contained in views.py and the api endpoints are defined in urls.py.

The configuration for the Admin app is in admin.py.